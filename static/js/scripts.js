//Setting up image handling to load into canvas
function drawShape(){
    var canvas = document.getElementById('numCanvas');
    var ctx = canvas.getContext('2d');
    ctx.fillRect(20, 20, 100, 100);
}